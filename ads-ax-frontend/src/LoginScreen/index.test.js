import React from 'react';
import { cleanup, render, fireEvent } from 'react-testing-library';
import 'jest-dom/extend-expect';

import Screen from './LoginScreenComponent';

afterEach(cleanup);

describe('The Login Screen Container', () => {
  describe('should...', () => {
    test('...', () => {

    });
  });
});
