import React from 'react';
import { cleanup, render, fireEvent } from 'react-testing-library';
import 'jest-dom/extend-expect';

import ShopPage from './ShopItemComponent';

afterEach(cleanup);

describe('The Shop Item Component', () => {
  describe('should fetch an API call on load', () => {

  });


});
